# Dockerfile.ui
FROM node:18-alpine
WORKDIR /app

# 1) GitHub Actions で生成された dist をコピー
COPY apps/agent-tars/dist ./dist

# 2) 実行時に必要な package.json をコピー
COPY apps/agent-tars/package.json ./

# 3) production 依存だけインストール
RUN npm install --production

# 4) これまでと同じ起動コマンド
CMD ["pnpm", "run", "start:web"]
